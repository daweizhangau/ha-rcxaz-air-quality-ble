# Test dependencies for the rcxaz_air_quality HA integration.

# HA test harness — pulls in homeassistant core as a test dependency
pytest-homeassistant-custom-component>=0.13.109
# HA Bluetooth retry connector (runtime dependency of the component)
bleak-retry-connector>=3.0.0
# BLE stack (transitive but pinned for reproducibility)
bleak>=0.21.0
# Async test support
pytest-asyncio>=0.23.0
# Transitive HA dependency (homeassistant.components.usb)
pyserial>=3.5
aiousbwatcher>=1.1.0
